pipeline{
    agent any

    stages{
        stage("install"){
            agent {docker{image "node:14.18.0"
                args '-u root:sudo'}
    }
            steps{
                sh "npm cache clean"
            // sh 'sudo chown -R 995:993 "/.npm"'
                sh "npm install"
            }
        }
    }
}